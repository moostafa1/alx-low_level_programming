/**
 * _abs - prints the absolute value of integer
 * @i: input integer to get its absolute
 *
 * Return: integer absolute
 */



int _abs(int i)
{
	if (i < 0)
	{
		return (i * -1);
	}
	return (i);
}
