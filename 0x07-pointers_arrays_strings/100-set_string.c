#include "main.h"

/**
 * set_string - sets the value of a pointer to a char
 * of integers
 * @s: pointer to the original string
 * @to: string we want to set its value to string a
 *
 * Return: void
 */
void set_string(char **s, char *to)
{
	*s = to;
}
